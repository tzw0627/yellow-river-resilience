# 本机运行说明

访问：http://127.0.0.1:8000

双击「启动平台.command」即可启动；启动后的终端窗口请保持打开，按 Ctrl+C 停止服务。
本机已安装 server/.venv（Python 3.12）和 web/node_modules，已从源码构建 web/dist。
前端、API 和静态数据统一由 FastAPI 在 8000 端口提供。

验证：前端构建通过，5 项后端单元测试通过；首页、汇总、孪生、地图边界、NDVI 叠加图、空间查询、规则问答及 PDF/Word 下载接口通过。

当前未配置大模型密钥，问答使用本地规则；如需接入真实模型，在 server/.env 中配置对应厂商参数，参考 server/.env.example，并重启服务。卫星底图需要网络。
源码包不包含完整原始/中间栅格，本次验证的是随包数据支持的平台运行，未重新执行离线遥感处理流程。
